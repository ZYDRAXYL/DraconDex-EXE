'use strict';
// Artisan module — the one-shot per-target template create flow that used
// to live here has been removed; the step-by-step wizard now reuses the
// existing module:create / classifier:createTemplate / author:createChapter /
// module:updateDescription IPC channels directly instead of a bespoke
// scaffolding transaction. Nothing left to export from this module.

module.exports = {};
